	// html渲染
	const r$CURRENT_SECONDS_UNIX = await(() => {
		const param1 = $1;	// todo add param description
		const param2 = $2;	// todo add param description
		const param3 = $3;	// todo add param description
		const param4 = $4;	// todo add param description
		const param5 = $5;	// todo add param description
		return aw5(param1, param2, param3, param4, param5)
	})();